spring:
  datasource:
    url: jdbc:mysql://localhost:3307/lv2_final_mission
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    url: jdbc:mysql://localhost:3307/lv2_final_mission
    user: root
    password: password
    baseline-on-migrate: true
    locations: classpath:db/migration

api:
  randommer:
    secret-key: 18de5e998c104ce19cf4ee107dd3c2c7
    host: randommer.io
    path:
      name: /api/Name

logging:
  level:
    org.springframework.web: DEBUG
    org.springframework.web.servlet.DispatcherServlet: DEBUG
